# Allow public packages to be fetched from npmjs.org
# while keeping private @cloudinary packages from CodeArtifact

# Use public registry for yoga-layout-prebuilt
yoga-layout-prebuilt:registry=https://registry.npmjs.org/

# Default registry (can be overridden by scope below)
registry=https://registry.npmjs.org/

# If you have private Cloudinary packages, uncomment and configure:
# @cloudinary:registry=https://cloudinary-232482882421.d.codeartifact.us-east-1.amazonaws.com/npm/cld-npm-store/
